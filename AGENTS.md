# DrLeeWorks 작업 지침

## 콘텐츠 구조

- 새 게시글과 software 콘텐츠는 반드시 콘텐츠별 slug 폴더를 만들고 그 안에 `index.md`를 작성한다. 관련 이미지도 가능하면 같은 폴더에 둔다.

## Git 커밋 및 푸시

- 사용자가 `푸시해줘`, `커밋하고 푸시해줘` 등 GitHub 반영을 명시적으로 요청한 경우에만 커밋하거나 푸시한다. 일반적인 코드 수정 요청만으로는 자동 커밋 또는 푸시하지 않는다.
- 커밋 대상을 Codex가 직접 수정한 파일로 한정하지 않는다. 먼저 전체 working tree의 `git status`와 diff를 확인한다.
- Obsidian에서 사용자가 직접 작성하거나 수정한 Markdown 문서와 게시글 이미지 등도 사이트에 필요한 변경인지 확인한다. 필요한 콘텐츠라면 같은 커밋에 포함한다.
- 특히 `src/content` 아래의 게시글, 이미지 및 관련 콘텐츠가 누락되지 않았는지 확인한다. Untracked 파일도 일괄 제외하지 말고 새 게시글이나 게시글용 이미지 등 배포에 필요한 파일인지 판단한다.
- `.obsidian/workspace.json`, `.obsidian/workspace-mobile.json`, 캐시, 임시 파일, 로컬 전용 파일 및 사이트 배포에 불필요한 개인 설정 파일은 임의로 커밋하지 않는다.
- 기존 `.gitignore`와 프로젝트 구조를 존중하며, 필요한 이유 없이 ignore 규칙을 대규모로 변경하지 않는다.
- 커밋 전에 의도하지 않은 파일과 개인정보, 비밀정보, 인증정보가 포함되지 않았는지 확인한다.
- 사용자가 특정 파일만 커밋하도록 지시한 경우에는 그 범위 지시를 우선한다.
- 푸시 후 커밋에 포함된 주요 변경사항과 push 결과를 간단히 보고한다.
